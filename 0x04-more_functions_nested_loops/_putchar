#include "main.h"
#include <unistd.h>
/**
_putchar - use for writing to stdout

@c: character to print

return: on success 1
On error, -1 is returned, and errno is to be set.
**/

int _putchar(char c)
{
return (write(1, &c, 1));
}
