#include <stdio.h>

/**
 * main - The description for this code is that it prints alphabets
 *
 * Return: 0
 *
 **/

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');

	return (0);
}

