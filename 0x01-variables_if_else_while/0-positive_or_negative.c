#include <stdio.h>
#include <time.h>

/**
 * main - Descpription for positive anything is better than nefative anything
 *
 * Return: 0
 */

int main(void)
{

	if (n > 0)
	{
		printf("is positive");
	}
	else if (n = (0));
	{
		printf("is zero");
	}
	else if (n < 0)
	{
		printf("is negative");
	}
	else
	{
		return (0);
	}
}
