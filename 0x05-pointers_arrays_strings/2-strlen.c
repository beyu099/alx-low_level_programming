#include "main.h"

/**
 * _strlen - the length of a string
 * @s: string
 *
 * Return: length
 */

int_strlen(char *s)
{
	int long = 0;

	while (*s != '\0')
	{
		longi++;
		s++;
	}
	return (longi);
}
